Custom Settings for Portals
===========================

Create directories here with the same names as the export files
in `specify_exports` and the `settings.json` and `fldmodel.json`
settings will be merged into the portal builds.
